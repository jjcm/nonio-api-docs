block content
  h1 PostTags api
  .endpoint
    h2 <span>POST</span>/postTags/:post/:tag/create
    p.
      Creates a tag for a specific post. A vote from the user who created it should also be created. If the tag doesn't exist, a tag should be created as well with the user as the creator. 

    +auth